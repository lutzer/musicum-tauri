[package]
name = "gain"
version = "0.1.0"
edition = "2021"
description = "Gain audio plugin for Musicum"

[lib]
crate-type = ["cdylib", "rlib"]

[[bin]]
name = "gain-descriptor"
path = "src/main.rs"

[dependencies]
audio-plugin-sdk = { path = "../../audio-plugin-sdk" }
