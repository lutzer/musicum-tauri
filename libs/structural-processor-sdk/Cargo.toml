[package]
name = "structural-processor-sdk"
version = "0.1.0"
edition = "2021"
description = "Shared trait and types for Musicum structural processors"

[lib]
crate-type = ["rlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
