[package]
name = "audio-plugin-sdk"
version = "0.1.0"
edition = "2021"
description = "Shared trait and macro for Musicum audio plugins"

[lib]
crate-type = ["rlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
hound = "3"
